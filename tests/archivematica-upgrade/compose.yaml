---
name: archivematica-upgrade-test

services:

  archivematica:
    build:
      args:
        UBUNTU_VERSION: "22.04"
    privileged: true
    security_opt:
      - seccomp:unconfined
      - apparmor:unconfined
    ports:
      - "2222:22"
      - "8000:80"
      - "8001:8000"
